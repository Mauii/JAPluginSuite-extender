# JAPluginSuite-extender
Adds a model browser that searches in all pk3 files and directories on disk for .glm files and imports them

This is pure experimental for the moment. Possible issues might be present until Blender comes with better FileBrowser utilities.
